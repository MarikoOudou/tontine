import 'package:flutter/material.dart';
    
class ScreenTransfert extends StatefulWidget {
  const ScreenTransfert({Key? key}) : super(key: key);

  @override
  _ScreenTransfertState createState() => _ScreenTransfertState();
}

class _ScreenTransfertState extends State<ScreenTransfert> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}